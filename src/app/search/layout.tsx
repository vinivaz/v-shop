export const metadata = {
  title: "Search | V-shop",
  description: "Search through our products and find what you need.",
};

export default function SearchLayout({ children }: { children: React.ReactNode }) {
  return children;
}