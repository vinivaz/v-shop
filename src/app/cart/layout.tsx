export const metadata = {
  title: "Cart | V-shop",
  description: "Review your selected products and prepare to checkout.",
};

export default function CartLayout({ children }: { children: React.ReactNode }) {
  return children;
}